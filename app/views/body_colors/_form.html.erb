<%= form_for(@body_color) do |f| %>
  <% if @body_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@body_color.errors.count, "error") %> prohibited this body_color from being saved:</h2>

      <ul>
      <% @body_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :visible %><br>
    <%= f.check_box :visible %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
