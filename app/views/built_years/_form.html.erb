<%= form_for(@built_year) do |f| %>
  <% if @built_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@built_year.errors.count, "error") %> prohibited this built_year from being saved:</h2>

      <ul>
      <% @built_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :gearbox %><br>
    <%= f.text_field :gearbox %>
  </div>
  <div class="field">
    <%= f.label :engine_displacement %><br>
    <%= f.number_field :engine_displacement %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
