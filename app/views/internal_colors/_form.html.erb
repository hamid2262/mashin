<%= form_for(@internal_color) do |f| %>
  <% if @internal_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internal_color.errors.count, "error") %> prohibited this internal_color from being saved:</h2>

      <ul>
      <% @internal_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :visible %><br>
    <%= f.check_box :visible %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
